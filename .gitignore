# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/
/native-web/

# production
/build

# misc
.DS_Store
*.pem
.tools/
/.claude/worktrees/
.codex/
.xhawk/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local

# generated artifacts
/artifacts

# App Store screenshots (large PNGs — upload to ASC manually, do not commit)
/screenshots/app-store

# Android build artifacts (keep android/ source, exclude build outputs)
/android/app/build/
/android/build/
/android/.gradle/
/android/app/.cxx/
/android/local.properties
/android/capacitor-cordova-android-plugins/

# iOS build artifacts (Xcode derived data)
/ios/build/

# Research / documentation PDFs (do not commit)
*.pdf

# App Store Connect API keys (NEVER commit — these grant App Manager access)
*.p8
AuthKey_*.p8
/fastlane/.keys/
